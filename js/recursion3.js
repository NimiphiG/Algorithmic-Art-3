


function setup() {
    createCanvas(800, 1200);
     background(100)
    stroke(255);
    noFill();
    noLoop(); // prevent draw() from looping
    drawCircle();
}

function drawCircle(x, y, d, count) {
   
  

}